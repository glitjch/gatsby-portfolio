// Testing env files only
// const production = {
//   url: process.env.NODE_PROD
// };
// const development = {
//   url: process.env.NODE_DEV
// };
// export const config = process.env.NODE_ENV === 'development' ? development : production;